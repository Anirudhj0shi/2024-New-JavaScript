//this is how u ittrate object
const Marvel ={
    moviename:'Infinity war',
    release:2017,
    hero:'Ironman',
    Rating:'4 Stars',
}

for (const key in Marvel) {
    // console.log(`${key} : ${Marvel[key]}`);
    // console.log(`${key} : ${Marvel[key]}`);
    console.log(`${key}`);
}